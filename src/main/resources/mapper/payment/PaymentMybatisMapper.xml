<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.biddingmate.biddinggo.point.mapper.PaymentMybatisMapper">

    <insert id="insert" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment(
            member_id,
            order_id,
            payment_key,
            payment_method,
            amount,
            status,
            approved_at,
            created_at
        ) VALUES (
            #{memberId},
            #{orderId},
            #{paymentKey},
            #{paymentMethod},
            #{amount},
            #{status},
            #{approvedAt},
            #{createdAt}
        )
    </insert>

    <select id="existsByOrderId" resultType="boolean" parameterType="String">
        SELECT EXISTS(
            SELECT 1
            FROM payment
            WHERE order_id = #{orderId}
        )
    </select>

</mapper>
