<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.biddingmate.biddinggo.notice.mapper.NoticeMapper">

    <insert id="insert" parameterType="Notice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice (
            title,
            content,
            status,
            created_at
        ) VALUES (
                     #{title},
                     #{content},
                     #{status},
                     #{createdAt}
                 )
    </insert>

    <update id="update" parameterType="Notice">
        UPDATE notice
        SET title   = #{title},
            content = #{content},
            status  = #{status}
        WHERE id = #{id}
    </update>

    <select id="findById" parameterType="long" resultType="Notice">
        SELECT id,
               title,
               content,
               status,
               created_at AS createdAt
        FROM notice
        WHERE id = #{id}
    </select>

</mapper>